% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_closest_match.R
\name{find_closest_match}
\alias{find_closest_match}
\title{Find Closest Matches in Two Character Vectors}
\usage{
find_closest_match(string, target_strings)
}
\arguments{
\item{string}{This is a character vector of species names in the observations that have no matching value in the verified species taxonomy database.}

\item{target_strings}{This is a character vector of the species names in the verified species taxonomy database to compare against.}
}
\value{
A data frame containing the original species names and their closest match from the verified species taxonomy database.
}
\description{
\strong{Deprecated as of WildObsR 0.2.0.} \code{find_closest_match()} will be removed in
a future release. The function still runs normally; it emits a warning once
per session (it is commonly passed to \code{sapply()}, so an unguarded warning
would fire once per element).

This function finds the closest match between species names provided in an unverified list
and a verified species taxonomy database. It handles differences in spelling or capitalization.
}
\details{
This function internally uses \code{sapply()} to apply string distance calculations
across all species names in the \code{string} vector. The closest match for each name is determined
using the Jaro-Winkler string distance method from the \code{stringdist} package.
}
\author{
Zachary Amir & ChatGPT
}
\keyword{internal}
