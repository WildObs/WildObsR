% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{convert_df_to_list}
\alias{convert_df_to_list}
\title{Convert a Data Frame to a Nested List}
\usage{
convert_df_to_list(df)
}
\arguments{
\item{df}{A data frame or a list containing a data frame.}
}
\value{
A list representation of the input data frame. If the input is a single-row data frame, it returns a named list. If the input is a list containing a data frame, it returns a list where each row is represented as a nested list.
}
\description{
This function is used internally in the wildobs_dp_download() function to convert a data frame into a nested list, ensuring that vector fields are flattened while preserving list-like structures.
}
\details{
The function performs the following transformations:
\itemize{
\item Converts the first row of a data frame into a named list.
\item If the input is a list containing a data frame, it processes each row separately and returns a list of lists.
\item Ensures that vector fields (e.g., \code{path}, \code{deploymentGroups}) are flattened to prevent unnecessary nested lists.
\item Stops execution if the input is neither a data frame nor a list containing a data frame.
}
}
\examples{
# Example with a single-row data frame
df <- data.frame(a = 1, b = "text", c = list(c(1, 2, 3)))
convert_df_to_list(df)

# Example with a list containing a data frame
df_list <- list(data.frame(a = 1:2, b = c("x", "y")))
convert_df_to_list(df_list)

}
\seealso{
\code{\link{wildobs_dp_download}} for downloading and bundling data packages.
}
\author{
Zachary Amir & ChatGPT
}
\keyword{internal}
